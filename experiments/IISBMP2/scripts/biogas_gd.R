# GD biogas

cbg.gd <- cumBg(biogas.vol, dat.type = 'vol', comp = comp.gd, comp.name = 'xCH4.gd', temp = 'temp.air', 
                pres = 'pres.amb', data.struct = 'long', id.name = 'id', time.name = 'time.d',
                dat.name = 'vol', temp.init = 20)
